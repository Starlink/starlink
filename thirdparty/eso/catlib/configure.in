# E.S.O. - VLT project 
# $Id: configure.in,v 1.1.1.1 2002/04/04 20:11:39 brighton Exp $
#
# This file is used with GNU autoconf to generate a configure script for
# the Skycat modules.
#
# usage: % autoconf
#        % configure
#        % make
# who             when       what
# --------------  --------   ---------------------------------------------
# Allan Brighton  27 Jul 98  Created
# P. Biereichel   12/06/99   Adapted for VLT OCT99 release
# -----------------------------------------------------------------------

AC_INIT(README)

#--------------------------------------------------------------------
#	run configure scripts in these subdirectories, if available
#--------------------------------------------------------------------
AC_CONFIG_SUBDIRS(tclutil astrotcl rtd cat skycat)

# according to the docs, subdirs should be set to the directories
# found, however it doesn't seem to work.
dirs=""
for i in $subdirs ; do
	if test -d $i; then
		chmod +w $i
		chmod +w $i/config.*
	fi
	test -d $i && dirs="$dirs $i"
done
subdirs=$dirs
AC_SUBST(subdirs)

AC_OUTPUT(Makefile)
