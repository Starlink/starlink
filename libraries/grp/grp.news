1 GRP_library
                                             Expires: ???

A new release (V3.6) of the GRP library is now available. It includes
the following changes:

- A new routine GRP_SLAVE to return the identifier for the group (if any)
that is owned by a specified group.
