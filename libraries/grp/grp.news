1 GRP_library
                                             Expires: ???

A new release (V3.2) of the GRP library is now available. It includes
the following changes:

 - Shell meta-characters (referneces to environment variables, for
instance) may now be included with file specifications used as
indirection elements (e.g. "^$DATA_DIR/myfiles").

