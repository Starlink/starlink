^$STARLINK_DIR/share/smurf/dimmconfig_bright_compact.lis

# This is the normal bright compact configuration with
# SMO used instead of FLT because the FLT processing
# can damage very short time series

modelorder = (com,gai,smo,ext,ast,noi)

apod = 0
flt.apod = 0

smo.type = mean
