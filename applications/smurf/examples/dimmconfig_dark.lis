^$STARLINK_DIR/share/smurf/dimmconfig.lis

# *** Specialized config for dark scans ***

# The purpose of this config is to measure the amplitude of the fridge
# fluctuations while the shutter is closed. A higher-order polynomial
# baseline is removed, but otherwise there is no special filtering of
# the data. The relative amplitudes of each detector are stored in
# the exported "GAI" model component. No extinction correction because
# we are not looking at the sky.
# ***********************************************************

# Don't do any filtering
modelorder = (com,gai,ast,noi)
exportndf = (com,gai)

# ----------------------------------------------------------------------------
# The following parameters control data-cleaning before iterations start
# ----------------------------------------------------------------------------

# No padding
padstart = 0
padend = 0

# No apodization
apod = 0

# higher-order polynomial for baselines
order = 5

# ----------------------------------------------------------------------------
# These parameters control the iterative model components
# ----------------------------------------------------------------------------

# weaker bolo rejection
com.corr_tol = 7
com.gain_tol = 7
com.gain_abstol = 5

# treat common-mode using a single chunk
com.gain_box = 600000

# weaker spike detection
noi.spikethresh = 50
noi.spikeiter = 0
