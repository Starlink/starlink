
Converted to work with running install-test.

Labels are now cleaned of any white space (include \t and \n).

html.perl modified to retain old \latexonly{} command.

Perl dependent scripts changes to work using version on PATH.

Fixed to kill forked process using -9, stop runaway on Linux.
