*+SLIST Common blocks for survey small map header/index records
	RECORD /HEADEF/ IHEAD, OHEAD
	RECORD /MAPDEF/ ILIST(NLON, NLAT), OLIST(NLON, NLAT)
	RECORD /MAPDEF/ IFREE, OFREE
	INTEGER ENDREC
	COMMON /SLIST/ IHEAD, OHEAD, ILIST, OLIST, IFREE, OFREE, ENDREC
	SAVE /SLIST/
