*+COM_DBS_FIELD     Include file, holds fieldnames, formats, etc.
 
      CHARACTER*17 FIELD(MAX_FIELD_COUNT, MAX_FILE_COUNT)	! Fieldnames
      COMMON /DBS_FIELD/ FIELD
 
      INTEGER NFIELDS(MAX_FILE_COUNT)				! No. in rec
      COMMON / DBS_NFLDS / NFIELDS
 
      CHARACTER*6 FORMAT(MAX_FIELD_COUNT, MAX_FILE_COUNT)	! Input fmts
      COMMON /DBS_FORMT/ FORMAT
 
      CHARACTER*6 NULFORMAT(MAX_FIELD_COUNT, MAX_FILE_COUNT)	! Output fmts
      COMMON /DBS_NULFM/ NULFORMAT
 
      INTEGER NKEYS(MAX_FILE_COUNT)				! no. of keyflds
      COMMON / DBS_NKEYS/ NKEYS
 
      CHARACTER*17 KEYFIELD(MAX_KEYFLD_COUNT, MAX_FILE_COUNT)	! Keyfields
      CHARACTER*1  ASCEND(MAX_KEYFLD_COUNT, MAX_FILE_COUNT)	! Order
      COMMON / DBS_ORDER / ASCEND, KEYFIELD

      CHARACTER*1 CONDITIONAL(MAX_FIELD_COUNT, MAX_FILE_COUNT)	! Null string or logical flag
      INTEGER COND_OFFSET(MAX_FIELD_COUNT, MAX_FILE_COUNT)	! Offset to field dependant upon
      COMMON /DBS_CONDITIONAL/ CONDITIONAL, COND_OFFSET
*-
