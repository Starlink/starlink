#+
#  Name:
#     awkvec

#  Purpose:
#     Extract cursor instructions from screen output generated by KAPPA:VECPLOT

#  Language:
#     awk script

#  Description:
#     This awk script suppresses the display of selected informational messages
#     produced by VECPLOT. 

#  Usage:
#     vecplot in1=... in2=... <etc> | awk -f awkvec

#  Notes:
#     -  This script makes assumptions about the contents of the messages
#     displayed by vecplot. If vecplot is changed, this script may need to be
#     changed too.

#  Authors:
#     DSB: David Berry (STARLINK)
#     {enter_new_authors_here}

#  History:
#     4-DEC-1993 (DSB):
#        Original version.
#     {enter_changes_here}
#-
{

#  
#  Initialise a flag to indicate that the line should be printed.
      on = 1
#
#  Suppress the printing of un-wanted lines.
      if( $1 == "Axis" && $4 == "non-linear" ) on = 0
      if( $0 == "the database will be wrong." ) on = 0
#
#  Print the line if it is wanted.
      if( on ) print $0
}
