# E.S.O. - VLT project/ESO Archive
# "@(#) $Id: Makefile.in,v 1.2 2005/02/02 01:43:02 brighton Exp $"
#
# Makefile.in
#
# This file is processed by configure to produce the actual Makefile.
#
# who             when       what
# --------------  ---------  ---------------------------------------------
# Allan Brighton  08 Mar 96  Created
# ------------------------------------------------------------------------

SRCS 		=  

OBJS 		=  

PROGS 		=  

INCLUDES	= -I../include \
		  $(BUILD_INCLUDES) \
		  -I$(TOPDIR)/include/astrotcl \
		  -I$(TOPDIR)/include/tclutil

CC_SWITCHES	= $(DEFINES) $(INCLUDES) $(CFLAGS)

LIBRARIES       = ../lib/librti.a \
		  $(ASTROTCL_LIB) \
		  $(TCLUTIL_LIB)


test: $(PROGS)

do_all:
do_depend:

lib:
	@cd ../src; $(MAKE)

do_clean: 
	$(RM) $(PROGS) $(OBJS) *\~ "#"* *.o

do_distclean: clean
	echo > make.dep
	rm -f Makefile	

tImageData: tImageData.o $(LIBRARIES)
	#$(CXX) $(CC_SWITCHES) -o $@ $@.o $(LIBRARIES) $(EXTRA_LIBS)	
	#-$@ > $@.result
	#-@if cmp $@.result $@.ok; \
	# then echo "$@: PASSED" ; \
	# else echo "*** $@: TEST FAILED: see $@.result"; \
	# fi

include ../../makefile.mk
# include ../../depend.mk


