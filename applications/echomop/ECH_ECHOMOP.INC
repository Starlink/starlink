      INTEGER ECH__MNMSIZ
      PARAMETER ( ECH__MNMSIZ = 48 )

      INTEGER NUM_OPTIONS
      PARAMETER ( NUM_OPTIONS = 32 )

      INTEGER NUM_SUBOPTIONS
      PARAMETER ( NUM_SUBOPTIONS = 30 )

      INTEGER NEXT_1D_OPTION( 0:NUM_OPTIONS )
      INTEGER NEXT_2D_OPTION( 0:NUM_OPTIONS )

      LOGICAL ONCE_PER_ORDER( 0:MAX_MODULES )

      INTEGER OPTIONS_IN_USE
      PARAMETER ( OPTIONS_IN_USE = 31 )

      INTEGER SPECIAL_OPTIONS_AT
      PARAMETER ( SPECIAL_OPTIONS_AT = 22 )

      INTEGER OPTION_STD_START
      PARAMETER ( OPTION_STD_START = 1 )

      INTEGER OPTION_STD_END
      PARAMETER ( OPTION_STD_END = 14 )

      INTEGER ADJUST_OPTION
      PARAMETER ( ADJUST_OPTION = 23 )

      INTEGER DISABLE_OPTION
      PARAMETER ( DISABLE_OPTION = 26 )

      INTEGER PLOT_OPTION
      PARAMETER ( PLOT_OPTION = 27 )

      INTEGER MENU_OPTION
      PARAMETER ( MENU_OPTION = 28 )

      CHARACTER*( ECH__MNMSIZ ) SUBMENU_OPTIONS( NUM_SUBOPTIONS )

      CHARACTER*( ECH__MNMSIZ ) SUBMENU_TEXT( NUM_SUBOPTIONS )

      CHARACTER*( ECH__MNMSIZ ) OPTION_MODULE( 0:NUM_OPTIONS )

      CHARACTER*( ECH__MNMSIZ ) OPTION_TEXT( 0:NUM_OPTIONS )

      COMMON / ECHOMOP_STUFF_1 / OPTION_MODULE,
     :                           OPTION_TEXT,
     :                           SUBMENU_TEXT,
     :                           SUBMENU_OPTIONS

      COMMON / ECHOMOP_STUFF_2 / NEXT_1D_OPTION,
     :                           NEXT_2D_OPTION,
     :                           ONCE_PER_ORDER
