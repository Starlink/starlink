*  Define common to hold reference feature database.
      INTEGER DB_SCOPE
      INTEGER MAX_FEATURES

      REAL SRC_FTR_LIST( MAX_FEATURES )
      REAL SRC_FTR_STRENGTH( MAX_FEATURES )

      REAL FTR_LIST( MAX_FEATURES )
      REAL FTR_STRENGTH( MAX_FEATURES )

      REAL FTR_DB( DB_SCOPE, DB_SCOPE, MAX_FEATURES )
      BYTE FTR_DB_INDEX_L( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      BYTE FTR_DB_INDEX_R( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      INTEGER*2 FTR_DB_INDEX_WAVE( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      BYTE FTR_DB_TEMP_L( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      BYTE FTR_DB_TEMP_R( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      INTEGER*2 FTR_DB_TEMP_WAVE( DB_SCOPE * DB_SCOPE * MAX_FEATURES )
      INTEGER FTR_DB_QUICK_INDEX( MAX_FEATURES )
      REAL FTR_DB_QUICK_VALUE( MAX_FEATURES )

      LOGICAL FTR_LIST_READ
      LOGICAL FTR_DB_OPEN

      CHARACTER*255 FTR_OPENED_LISTS 

      COMMON / FTR_DBSTATE / FTR_LIST_READ, FTR_DB_OPEN

      COMMON / FTR_DBNAMES / FTR_OPENED_LISTS
